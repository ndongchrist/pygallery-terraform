[project]
name = "pygallery-terraform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ndongchrist", email = "christianhonore2003@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
pygallery-terraform = "pygallery_terraform:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
